# Virtual Environment
env/

# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class$

# Firebase Admin Key (CRITICAL!)
firebase-admin-key.json

# Render specific (optional, but good practice)
.render-build.sh # If you have one and don't want to track changes

# Other common Django ignores (optional)
*.log
local_settings.py
db.sqlite3
media/
staticfiles/